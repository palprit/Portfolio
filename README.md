
  # High-Impact UX Portfolio Design

  This is a code bundle for High-Impact UX Portfolio Design. The original project is available at

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
